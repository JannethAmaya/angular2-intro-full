/* tslint:disable:no-unused-variable */

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import {Prize} from './prize';

describe('Prize', () => {
  it('should create an instance', () => {
    expect(new Prize()).toBeTruthy();
  });
});
